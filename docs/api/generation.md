# `llm.generation` — Sampling and Backends

Token sampling and the generation backend abstraction. The
backend is what the serving tier and the trainer's evaluation loop
both call into.

## Sampling Utilities

::: llm.generation.sampling

## Generation Backend ABC

::: llm.generation.backends

## Backend Registry

::: llm.generation.registry

## Eager (Streaming) Backend

::: llm.generation.eager
