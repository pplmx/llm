# `llm.tokenization` — Tokenizer Implementations

Support for HuggingFace tokenizers and a built-in character-level
tokenizer for simple experiments.

## Overview

| Module             | Purpose                      |
| ------------------ | ---------------------------- |
| `tokenizer`        | Base tokenizer interface     |
| `simple_tokenizer` | Character-level tokenizer    |
| `bpe_tokenizer`    | Byte-Pair Encoding tokenizer |
| `train_bpe`        | BPE training utilities       |

## Base Tokenizer

::: llm.tokenization.tokenizer

## Simple Character Tokenizer

::: llm.tokenization.simple_tokenizer

## BPE Tokenizer

::: llm.tokenization.bpe_tokenizer

## BPE Training

::: llm.tokenization.train_bpe
