[project]
name = "llm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.4.0",
    "datasets>=3.3.1",
    "peft>=0.14.0",
    "torch>=2.6.0",
    "transformers>=4.49.0",
]
